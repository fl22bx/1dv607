using System;
using System.Xml;
using System.Xml.Linq;

namespace dv607workshop2.models
{
    public static class XmlWritter
    {
        public static void addBoat (this Boat boat)
        {
            // write to xml
        }

        public static void AddUser (this string username, long personalNumber)
        {

            // member memberToSave = new member(username, personalNumber);
            // memberToSave.Save("register.xml");

        }
    }
}
